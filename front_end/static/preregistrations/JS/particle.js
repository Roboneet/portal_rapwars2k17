$(document).ready(function() {
  $('#wrapper').particleground({
    // dotColor: '#5cbdaa',
    // lineColor: '#5cbdaa',
    dotColor: '#cc7700',
    lineColor: '#cc7700',
   	density: 9000,
  });
 
});